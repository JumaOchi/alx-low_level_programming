#include "main.h"
/**
* _isupper - checks for uppercase characters
*
* Description: check character
*@c: Character
*Return: Integer
*/

int _isuppper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
